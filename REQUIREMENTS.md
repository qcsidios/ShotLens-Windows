# ShotLens for Windows 产品与技术需求规格

> 状态：待最终审核
> 设计确认日期：2026-06-28
> 目标版本：Windows `v0.2.0`
> 产品基线：macOS 版 ShotLens `v0.8.7`，仓库提交 `ea0fdc6`

## 1. 文档目的

本文件是 ShotLens Windows 重构的唯一需求基线。Windows 版不再沿用“在现有实现上持续补丁”的开发方式，而是以当前 macOS 版的功能、界面层级、交互和结果质量为产品基准，重新实现适合 Windows 的技术路线。

代码实现、测试和发布必须能追溯到本文中的需求编号。未达到发布验收标准前，不创建正式 GitHub Release。

## 2. 已确认的产品决策

1. Windows 版产品名统一为 `ShotLens`，不显示“ShotLens for Windows”。
2. 直接复用 macOS 版正式 Logo，不为 Windows 重新设计图标。
3. 主界面保持 macOS 版的信息层级和功能顺序，仅进行必要的 Windows 系统适配。
4. Windows 没有对应屏幕录制授权限制时，不显示“屏幕录制权限”板块。
5. 采用方案 B：保留 WPF 应用外壳，重新构建界面和核心流程，只复用经过验证的设置、托盘、升级和安装器能力。
6. 默认截图快捷键为 `Ctrl + Alt + S`，用户可在界面中修改。
7. 点击主窗口右上角关闭按钮时缩小到托盘，不退出进程。
8. 设置自动保存，不保留 Windows 旧版独立“保存”按钮。
9. OCR 首版只内置中文和英文能力，以控制安装包体积。
10. 默认限免 Key 不在界面、设置文件、日志或错误信息中显示；用户自己填写的 Key 可以通过显隐按钮查看。
11. 最终只交付一个中文 `Setup.exe`，支持 Windows 10/11 x64，不提供 ZIP。
12. 重构期间不发布正式版本；全部核心能力通过验收后再发布 Windows `v0.2.0`。
13. Windows Release 只上传 Windows 安装包，不重新打包没有变化的 macOS DMG。
14. 每个 Release 的发布说明只写相对上一 Windows 版本的本次变化，不重复历史内容。
15. 用户自定义 API Key 仅以明文保存在本机 `%APPDATA%\ShotLens\settings.json`；界面需要明确提示本地明文存储，不上传到 ShotLens 服务。
16. 默认限免配置使用硅基流动 API。默认 Key 通过 GitHub Actions Secret `SHOTLENS_DEFAULT_API_KEY` 在发布构建时注入安装包；用户安装后无需配置即可使用默认限免。
17. 阶段 0 不要求用户提供 OCR 截图。项目自行生成可重复的中英文 UI/网页样本进行候选路线对比；真实使用样本延后从 beta 反馈中补充。
18. 开发期 beta 与稳定版并行安装：beta 使用独立 AppId、`ShotLens Beta` 安装目录和设置目录。用户只需首次手动安装 beta.1，之后所有 beta 在应用内覆盖升级；接近正式版时再验证 v0.1.5 到 v0.2.0 候选版的稳定通道升级。
19. beta 安装包命名为 `ShotLens-Beta-vX.Y.Z-beta.N-Setup.exe`，避免旧版稳定更新器误发现 Pre-release。
20. 开机自动启动时只进入托盘，不自动显示主窗口。
21. 默认硅基流动地址为 `https://api.siliconflow.cn/v1`，默认模型为 `tencent/Hunyuan-MT-7B`；真实 API 测试失败时再通过需求变更调整。
22. 当前没有 Windows 代码签名证书；开发版和首个正式版允许显示未知发布者提示，但不得通过降低其他验收标准规避。

## 3. macOS 产品基线

以下实现是 Windows 需求的事实来源：

- `ShotLens/App/MainWindow.swift`：主窗口、设置、更新和 API 交互。
- `ShotLens/App/ShotLensApp.swift`：菜单栏、快捷键、截图到翻译的主流程。
- `ShotLens/Core/ScreenshotCapture.swift`：冻结屏幕、跨显示器捕获和裁剪。
- `ShotLens/Core/InProcessSelectionOverlay.swift`：选区覆盖层和坐标转换。
- `ShotLens/Tools/ShotLensOCR.swift`：独立 OCR 辅助进程。
- `ShotLens/Core/TextLayoutOptimizer.swift`：文本块过滤、分类和合并。
- `ShotLens/Core/LLMTranslator.swift`：翻译请求、结果校验和容错。
- `ShotLens/Core/OverlayWindow.swift`：原位结果层、重试、切换和复制。
- `ShotLens/Core/AppUpdater.swift`：平台安装包检测、下载和安装。

Windows 版需要达到的 macOS 基线能力：

- 常驻系统菜单区域，支持全局快捷键。
- 冻结当前屏幕并进行准确框选。
- 本地 OCR，截图像素不发送给翻译 API。
- 自动判断文本布局，保持文本块顺序。
- 将 OCR 文字批量发送到兼容 OpenAI 的 API。
- 翻译结果严格覆盖到原 OCR 位置。
- 支持原文/译文切换、重试、复制截图和译文。
- 支持自定义 API、默认限免、API 测试和自动保存。
- 支持应用内检测、下载并安装新版本。

## 4. 平台与交付范围

### 4.1 支持范围

- Windows 10 版本 2004（Build 19041）或更高版本。
- Windows 11。
- x64 Intel/AMD 处理器。
- CPU 模式必须可用，不要求独立显卡、CUDA 或 NPU。
- ARM64 不属于 `v0.2.0` 范围。

### 4.2 安装包

- 文件名：`ShotLens-Windows-v0.2.0-Setup.exe`。
- 中文安装向导、开始菜单项和卸载信息。
- 安装包自带应用运行所需组件和中英文 OCR 模型。
- 用户不需要预装 .NET、Python、OCR 语言包或 CUDA。
- 不发布 ZIP、便携版或单独模型包。
- 体积目标为 70–120 MB；最终以通过准确率和启动测试后的实际产物为准。
- 体积优化不得以牺牲 OCR 准确率、首次启动可用性或离线 OCR 为代价。

## 5. 主界面与系统交互

### UI-001 主窗口

- 窗口标题和产品名称均为 `ShotLens`。
- 左上区域依次显示正式 Logo、产品名和版本信息。
- 产品名必须与 Logo 紧贴并左对齐，不因 API 展开而改变位置。
- 使用 Windows 原生窗口控件、Segoe UI 和 Microsoft YaHei UI。
- 控制内容宽度和留白，保持 macOS 版的紧凑卡片风格。
- 删除无功能价值的大段解释文字。

### UI-002 信息顺序

主窗口从上到下固定为：

1. Logo、ShotLens、版本、检测新版本和更新状态。
2. 快捷键卡片。
3. 开机自动启动卡片。
4. API 信息卡片。
5. “设置会自动保存”和“开始截图”主按钮。

Windows 没有独立屏幕录制权限时，不显示权限卡片。

### UI-003 快捷键卡片

- 标题：“快捷键”。
- 辅助文字：“按下后直接进入截图框选。”。
- 默认显示 `Ctrl + Alt + S`。
- 点击“设置”后进入录制状态。
- 录制时全局快捷键暂时停用，避免触发截图。
- 新组合键注册成功后立即保存并恢复全局监听。
- 组合键冲突时保留旧快捷键，并在本卡片显示错误。
- 不允许仅使用单个字母、数字或无修饰键的快捷键。

### UI-004 开机启动卡片

- 标题：“开机自动启动”。
- 辅助文字：“登录 Windows 后自动启动 ShotLens。”。
- 开关变化后立即写入当前用户启动项。
- 开机启动失败时恢复原状态并显示错误。

### UI-005 API 信息收起状态

- API 信息默认收起。
- 收起状态显示标题、当前配置状态和“展开”按钮。
- 配置状态至少包含“使用默认配置”“使用自定义配置”“配置不完整”“API 可用”“API 不可用”。

### UI-006 API 信息展开状态

展开后依次显示：

1. 地址。
2. Key。
3. 模型。
4. “清空”“恢复默认”“测试”。
5. 一行简短的默认配置说明。

交互要求：

- 展开 API 卡片时只增加窗口高度，不改变头部和其他卡片的对齐。
- 地址和模型可输入。
- 模型支持输入和预设选择。
- 用户 Key 默认掩码显示，可通过眼睛按钮切换。
- 默认限免 Key 永远不填充到输入框，也不能通过眼睛按钮显示。
- “清空”清除全部自定义值并禁用默认回退。
- “恢复默认”恢复默认地址、模型和限免配置。
- “测试”必须发起真实的聊天补全请求。
- 测试期间按钮禁用，完成后在 API 卡片内显示结果。
- 所有设置在输入或操作后自动保存。

### UI-007 更新状态

- “检测新版本”是版本号旁的文字按钮。
- 状态依次支持“检查中…”“已是最新版”“发现 X.Y.Z”“无法连接更新服务器”“升级失败”。
- 已是最新版时不显示 GitHub 仓库或 Release 链接。
- 发现新版时显示“升级”按钮。
- 请求进行中不得重复触发。

### APP-001 单实例

- 同一用户会话只能运行一个 ShotLens 实例。
- 再次启动时唤起已有主窗口。
- 单实例实现不得阻塞 UI 线程。

### APP-002 托盘与退出

- 启动后创建 ShotLens 托盘图标。
- 托盘菜单包含“显示 ShotLens”“开始截图”“退出 ShotLens”。
- 点击窗口关闭按钮只隐藏窗口。
- 只有托盘“退出 ShotLens”或安装器升级流程可以结束进程。
- 托盘、全局快捷键和后台更新状态在主窗口隐藏后继续工作。

## 6. 截图与框选

### CAP-001 截图触发

- “开始截图”、默认快捷键和托盘菜单进入同一条截图流程。
- 正在截图、OCR 或翻译时再次触发，不创建重复流程。
- 触发后隐藏主窗口，再捕获屏幕。

### CAP-002 冻结屏幕

- 使用 DXGI Desktop Duplication 按显示器捕获桌面原始帧。
- 不再使用 `Graphics.CopyFromScreen` 捕获并拉伸整个虚拟桌面。
- 每块显示器保留自己的物理像素尺寸、屏幕坐标和 DPI。
- 捕获发生在选区覆盖窗口出现之前。
- 不捕获鼠标指针、选区蒙版或 ShotLens 主窗口。

### CAP-003 多显示器与 DPI

- 进程使用 Per-Monitor V2 DPI awareness。
- 每块显示器创建独立的无边框选区窗口。
- 覆盖窗口严格使用对应显示器的屏幕边界。
- 支持主屏和副屏位于负坐标的排列。
- 支持 100%、125%、150% 和 200% 缩放组合。
- 选区逻辑坐标必须使用所在显示器的 DPI 转换为物理像素。
- 一个选区只属于一块显示器，不跨显示器拖动。

### CAP-004 选区交互

- 冻结截图作为选区窗口背景。
- 未选择区域覆盖约 36% 黑色蒙版。
- 当前选区显示白色细边框，选区内部保持清晰。
- 支持任意方向拖动。
- 最小有效区域为 20×20 逻辑像素。
- 按 Esc 或无效点击取消。
- 取消后不自动弹出主窗口，只恢复托盘待机状态。

### CAP-005 裁剪

- 松开鼠标后，从已冻结的显示器帧直接裁剪。
- 不进行第二次屏幕截图。
- 裁剪采用向外取整并限制在物理图像边界内。
- 屏幕四边必须能选到最后一个物理像素。
- 裁剪结果不得包含蒙版、选区边框或缺失边缘。

## 7. OCR

### OCR-001 架构

- OCR 在独立辅助进程中执行，主 WPF 进程不直接加载 OCR 原生运行时。
- 主进程通过文件或标准输入传入选区图像。
- 辅助进程输出结构化 JSON。
- OCR 崩溃、超时或输出损坏不能导致 ShotLens 主进程退出。
- 主进程必须设置超时并清理临时文件。

### OCR-002 技术选型

- 使用成熟的 PaddleOCR 模型体系。
- 优先采用 ONNX Runtime 与 PP-OCR Mobile 模型，避免打包 Python、CUDA 和完整 Paddle 运行时。
- 首版只内置中文和英文识别模型。
- 最终接入层不能只凭仓库 Star 决定。
- 实现前必须使用相同测试集比较至少两条可行路线，例如 PaddleSharp 和独立 PaddleOCR JSON 辅助进程。
- 比较指标包括文字行召回率、字符准确率、顺序、耗时、内存、安装体积和异常隔离。
- 达不到验收标准的路线不得进入正式包。

### OCR-003 输出模型

每个 OCR 文本块至少包含：

- 原文。
- 物理像素坐标边界。
- 置信度。
- 检测语言。
- 估算字号。
- 前景与背景亮度信息。

### OCR-004 小字与预处理

- 保留原始选区图像用于最终显示。
- 小字号或低分辨率内容可以在 OCR 前放大。
- OCR 坐标必须按相同比例映射回原图。
- 预处理不得改变最终显示截图。
- 不使用会吞掉细字体、标点或浅色文字的激进二值化。

## 8. 布局分析与翻译

### LAYOUT-001 文本过滤

- 去除空文本、纯标点和高概率图标噪声。
- 不能仅因置信度略低就丢弃包含有效文字的整行。
- 保留 OCR 原始顺序信息。

### LAYOUT-002 布局分类

移植 macOS `TextLayoutOptimizer` 的行为，至少支持：

- 短文本。
- 菜单列表。
- 段落。
- 文章。
- 混合布局。

菜单和短文本保持独立行；段落和文章按位置、间距、字号、亮度和对齐方式合并。不同标题层级不得错误合并。

### TRANS-001 API 配置

- 支持兼容 OpenAI 的聊天补全接口。
- 地址可接受 `/v1`、`/v1/`、`/v1/chat/completions` 和 `/v1/models` 等常见输入，并规范化为正确端点。
- 模型有值时显式传入，留空时允许服务端采用默认模型。
- 用户设置存储在 `%APPDATA%\ShotLens\settings.json` 或等效的当前用户目录。
- 用户自定义 Key 以明文保存在该本地设置文件中，不上传到 ShotLens 服务。
- API 卡片需要提示“自定义 Key 仅保存在本机设置文件中”。

### TRANS-002 默认限免 Key

- 默认限免 Key 不写入用户设置。
- 默认限免 Key 不显示在 UI、日志、崩溃日志、发布说明或测试输出中。
- 默认限免 Key 只在请求构造时使用。
- 用户自定义 Key 覆盖默认 Key。
- 本要求只保证产品层不可见，不承诺阻止二进制逆向或网络层提取。
- 默认限免 Key 来自发布环境的 GitHub Actions Secret `SHOTLENS_DEFAULT_API_KEY`。
- Release 构建缺少该 Secret 时必须失败；普通 CI、Fork PR 和本地开发构建不要求该 Secret。
- 本地开发构建没有默认 Key 时仍可使用用户自定义 API。

### TRANS-003 翻译流程

- 自动检测 OCR 文本的主要源语言。
- 目标语言跟随 Windows 当前用户的首选显示语言；无法取得时回退为简体中文。
- 按 macOS 规则批量发送文本块。
- 优先要求 API 返回与输入一一对应的 JSON 数组。
- 输入和输出文本块数量必须完全一致后才能渲染。
- 支持修复代码块、编号列表、对象包装、解释前缀和常见字段名。
- 检测拒答、安全判定或非翻译内容，并对对应文本块重试。
- 批量失败时按单项降级重试。
- 单项仍失败时显示“翻译失败”，不得将错误文本覆盖到截图。

### TRANS-004 API 测试

- API 测试使用极小的真实聊天补全请求。
- 不以 `/models` 是否可访问作为唯一可用判断。
- 测试成功必须证明当前地址、Key 和模型能完成一次最小请求。
- 错误信息经过简化，不显示 Key 或完整敏感请求。

## 9. 原位结果层

### RESULT-001 窗口结构

- 结果层位于原选区的屏幕位置。
- 窗口逻辑尺寸由截图物理像素和对应显示器 DPI 计算。
- 显示截图时不得二次缩放造成模糊。
- 结果窗口无系统边框、置顶、可拖动。
- 结果窗口之外显示透明背景层，用于接收关闭交互。

### RESULT-002 处理状态

- OCR 开始时显示“正在识别”。
- 翻译开始时显示“正在翻译”。
- 成功时显示“翻译完成”和“显示原文”。
- 失败时显示简短错误和“重新翻译”。
- 状态控件放在结果窗附近，不遮挡截图主体。

### RESULT-003 译文渲染

- 每个译文块使用其对应 OCR 物理坐标。
- 译文覆盖区域必须包含在原 OCR 区域和截图边界内。
- 使用接近不透明的浅色背景保证可读性。
- 字号根据原始字号、文本长度和可用区域自动缩小。
- 译文按字符换行。
- 标题和正文保持稳定的字号层级。
- 译文块不能越界、互相覆盖或错配到其他原文。
- 无法在区域内可读显示时，应优先缩小字号或保留原文，不得扩展覆盖到相邻内容。

### RESULT-004 结果交互

- “显示原文”和“显示翻译”在同一结果窗口切换。
- 保存按钮将当前渲染截图和按阅读顺序排列的译文文本同时写入剪贴板。
- 复制成功后关闭本次结果层。
- 点击结果窗口外部或按 Esc 关闭。
- 失败状态支持重新翻译，不重新截图和 OCR。

## 10. 更新、版本和发布

### UPDATE-001 更新检测

- 查询 GitHub Releases，但只将包含 `ShotLens-Windows-vX.Y.Z-Setup.exe` 的 Release 视为 Windows 更新。
- 忽略只有 DMG 的 macOS Release。
- 使用语义化版本比较，不使用字符串字典序。
- 已是最新版时不返回或显示 Release URL。

### UPDATE-002 下载与安装

- 点击“升级”后在应用内下载 Windows 安装包。
- 显示下载进度。
- 下载到系统临时目录。
- 至少验证 HTTP 成功、文件非空、文件名和目标版本匹配；发布流程提供校验和时还需校验 SHA-256。
- 下载完成后启动中文安装器的静默升级模式。
- 安装器关闭旧进程、覆盖安装并重新启动 ShotLens。
- 用户不需要先卸载旧版本。
- 升级必须保留快捷键、开机启动和 API 设置。

### VERSION-001 版本策略

- macOS 和 Windows 使用同一套三段式 SemVer 格式，但按平台安装包独立形成更新通道。
- Windows 重构稳定版使用 `v0.2.0`。
- `v0.2.0` Release 只上传 Windows Setup.exe。
- macOS 更新器只识别 DMG，Windows 更新器只识别 Setup.exe。
- 开发过程不创建正式 Release；测试构建作为 CI Artifact 保存。
- 后续修复使用 `v0.9.1`、`v0.9.2`；新增较大功能使用下一个 minor 版本。

### RELEASE-001 GitHub Release

- Release 中只保留平台安装包，不上传 ZIP。
- Windows 安装包名称必须包含完整版本。
- 发布说明只描述当前版本相对上一 Windows 版本的变化。
- 发布说明不得复制历史版本条目。
- GitHub Release 发布前必须完成第 12 节的全部发布门槛。

### RELEASE-002 代码签名

- 无 Windows 代码签名证书时，安装包可能显示“未知发布者”或 SmartScreen 提示。
- 代码签名证书属于消除系统警告的外部依赖，不用降低功能验收标准来绕过。
- 如果提供证书，CI 和本地发布流程必须对主程序、OCR 辅助进程和安装包签名。

## 11. 技术架构边界

### 11.1 保留

- WPF 主应用。
- 当前用户设置目录和可迁移的设置字段。
- 已验证的托盘服务、单实例思路、更新下载逻辑和 Inno Setup 安装框架。

### 11.2 重建

- 主窗口 XAML 和交互状态。
- 截图捕获。
- 多显示器选区覆盖层。
- DPI 与物理像素坐标模型。
- OCR 接入和辅助进程。
- 文本布局优化。
- 结果层渲染和交互。
- 应用流程编排和可取消状态。

### 11.3 模块职责

- `ApplicationShell`：单实例、主窗口、托盘、退出生命周期。
- `SettingsStore`：设置读写、迁移和默认值。
- `HotKeyService`：注册、注销和冲突反馈。
- `CaptureService`：显示器枚举和 DXGI 冻结帧。
- `SelectionOverlay`：每屏窗口、选区和 DPI 转换。
- `OcrWorkerClient`：启动辅助进程、超时、JSON 校验。
- `TextLayoutService`：过滤、排序、分类和合并。
- `TranslationService`：API 请求、解析、校验和重试。
- `ResultOverlay`：状态、原位绘制、切换、复制和关闭。
- `UpdateService`：平台 Release 检测、下载和启动安装。

模块之间只传递明确的数据对象，不共享 WPF 控件状态。截图物理坐标必须从捕获到 OCR、布局和渲染保持同一个坐标系。

## 12. 验收与发布门槛

### 12.1 自动化测试

- API 地址规范化测试。
- SemVer 与平台 Release 过滤测试。
- 翻译 JSON、编号列表、对象包装和数量错误解析测试。
- OCR JSON 输出和超时测试。
- 文本布局分类与合并测试。
- DPI 逻辑坐标到物理像素的转换测试。
- 裁剪边界和负坐标测试。
- 译文框不得超出截图边界的属性测试。
- 设置迁移、自动保存和默认 Key 不落盘测试。

### 12.2 OCR 基准集

建立可重复的本地测试集。阶段 0 由项目生成和维护，至少包含：

- 30 张英文 UI 或网页渲染图。
- 20 张中文 UI 或网页渲染图。
- 10 张中英混合渲染图。
- 浅色、深色、不同字号、不同缩放和密集段落。
- beta 阶段发现的典型漏识别截图作为回归样本逐步补充。

阶段 0 的生成样本必须使用真实字体与实际渲染引擎，保存生成参数和期望文本，保证两条 OCR 路线使用完全相同的输入。生成样本用于技术选型，不得冒充真实用户场景统计；正式发布结论还需要结合 beta 实际使用反馈。

发布指标：

- 清晰且字号不小于 12 物理像素的英文字符准确率不低于 97%。
- 清晰且字号不小于 12 物理像素的中文字符准确率不低于 95%。
- 有效文字行召回率不低于 95%。
- 不允许出现遗漏整段主要正文的样本。
- 同一基准集不得比当前 macOS 版低超过 2 个百分点；达不到时需记录差异并继续优化。

### 12.3 性能基准

在发布前记录一台无独立显卡的 Windows x64 参考机配置，并满足：

- 快捷键触发到冻结层可交互：目标不超过 300 ms。
- 普通 1920×1080 选区本地 OCR：中位数不超过 1.5 秒，P95 不超过 3 秒。
- 布局整理和结果层首次绘制：不超过 200 ms。
- 网络翻译耗时单独统计，不掩盖本地性能。
- OCR 辅助进程结束后内存可回收，不长期驻留大模型内存。

### 12.4 Windows 真实环境矩阵

至少验证：

- Windows 10 22H2 x64。
- 最新稳定 Windows 11 x64。
- 单屏 100%、125%、150%、200%。
- 双屏相同 DPI。
- 双屏不同 DPI。
- 副屏位于主屏左侧或上方的负坐标。
- 无独立显卡。
- 干净系统首次安装。
- 从 Windows `v0.1.5` 应用内升级到 `v0.2.0`。
- 升级后设置保留、程序自动重启、快捷键和托盘仍可用。

### 12.5 UI 与行为验收

- 主界面信息顺序与本文一致。
- Logo 使用 macOS 正式资源，产品名与 Logo 左对齐。
- API 默认收起，展开后不破坏其他布局。
- 检测更新、API 测试、快捷键设置和开始截图均有真实效果和状态反馈。
- 点击关闭按钮后进程仍在托盘运行。
- 托盘退出能完整释放快捷键和后台进程。
- 默认限免 Key 无法从界面、设置和日志看到。

### 12.6 截图与结果验收

- 冻结画面与原屏清晰度一致。
- 选区蒙版不进入最终截图。
- 四边不缺像素。
- 不同 DPI 显示器上选区、裁剪和结果位置一致。
- OCR 文本块、翻译输入、翻译输出和渲染块数量一一对应。
- 所有译文框位于截图边界内。
- 原文/译文切换、重试、复制和 Esc 关闭可用。

### 12.7 Release 前置条件

只有以下条件全部满足才能发布：

1. 自动化测试通过。
2. OCR 基准达标。
3. Windows 10/11 真实环境测试通过。
4. 从上一 Windows 版本应用内升级成功。
5. 中文 Setup.exe 在干净系统安装并启动成功。
6. Release 中只有 Setup.exe，无 ZIP。
7. 发布说明只包含 `v0.2.0` 本次变化。
8. 未发现默认限免 Key 泄漏。

## 13. 实施顺序

### 阶段 0：测试与原型

- 建立 OCR 基准集和评分脚本。
- 验证 DXGI 多屏捕获与 DPI 坐标。
- 比较 OCR 接入候选路线。
- 记录预计安装包体积和运行时依赖。

### 阶段 1：应用外壳和主界面

- 单实例、托盘和关闭到托盘。
- 重做主窗口。
- 快捷键录制与冲突处理。
- 开机启动。
- API 自动保存、收起/展开和测试。

### 阶段 2：截图与选区

- DXGI 按显示器冻结帧。
- 每屏独立覆盖窗口。
- Per-Monitor V2 坐标转换。
- 精确裁剪和取消流程。

### 阶段 3：OCR、布局与翻译

- OCR 辅助进程。
- 中英文模型。
- 布局分类和合并。
- 移植 macOS 翻译容错。

### 阶段 4：结果层

- 原位渲染。
- 处理与失败状态。
- 原文/译文切换。
- 重试、拖动、复制和关闭。

### 阶段 5：安装、升级与发布验证

- 中文单文件 Setup.exe。
- 应用内下载、覆盖安装和重启。
- Windows 真实环境矩阵。
- Release 门槛检查。

## 14. 非目标

`v0.2.0` 不包含：

- ARM64。
- GPU/CUDA 专用加速包。
- Python 运行环境。
- 中英文之外的 OCR 模型。
- 云端 OCR。
- 便携 ZIP。
- 自动截图历史、账号系统或云同步。
- 与本次 Windows 重构无关的 macOS 功能修改。

## 15. 完成定义

“完成”不是代码能够编译，也不是 CI 生成了安装包。只有真实 Windows 电脑能够完成以下完整流程，才算完成：

1. 双击中文 Setup.exe 安装。
2. 启动并看到正确主界面。
3. 设置或使用默认快捷键。
4. 冻结屏幕并精确框选。
5. 完整识别中英文文字。
6. 成功翻译并在正确位置渲染。
7. 切换、重试和复制结果。
8. 关闭到托盘并再次唤起。
9. 在应用内检测下一测试版本并完成覆盖升级。
10. 升级后配置不丢失、程序可重新启动。

任何一项失败，都不能发布 `v0.2.0`。

<Window x:Class="ShotLens.Windows.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ShotLens"
        Width="560"
        Height="560"
        MinWidth="460"
        MinHeight="500"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Border Width="44"
                    Height="44"
                    CornerRadius="12"
                    Background="{StaticResource ShotLensCardBrush}"
                    BorderBrush="{StaticResource ShotLensBorderBrush}"
                    BorderThickness="1"
                    Margin="0,0,12,0">
                <Image x:Name="LogoImage" Width="30" Height="30"/>
            </Border>
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="ShotLens" FontSize="24" FontWeight="SemiBold"/>
                <TextBlock x:Name="VersionTextBlock" Foreground="#667085" Margin="0,3,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Name="CheckUpdateButton" Content="检测新版本" Click="CheckUpdateButton_Click"/>
                <Button x:Name="InstallUpdateButton" Content="升级" Click="InstallUpdateButton_Click" Visibility="Collapsed"/>
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Border Background="{StaticResource ShotLensCardBrush}"
                        BorderBrush="{StaticResource ShotLensBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="16"
                        Margin="0,0,0,14">
                    <StackPanel>
                        <TextBlock Text="快捷键" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border BorderBrush="{StaticResource ShotLensBorderBrush}"
                                    BorderThickness="1"
                                    Background="White"
                                    CornerRadius="6"
                                    Padding="10,7"
                                    Margin="0,0,10,0">
                                <TextBlock x:Name="ShortcutTextBlock"/>
                            </Border>
                            <Button Grid.Column="1" x:Name="RecordShortcutButton" Content="设置" Click="RecordShortcutButton_Click"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource ShotLensCardBrush}"
                        BorderBrush="{StaticResource ShotLensBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="16">
                    <StackPanel>
                        <TextBlock Text="API 信息" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,12"/>
                        <TextBlock Text="API 地址"/>
                        <TextBox x:Name="EndpointTextBox"/>

                        <TextBlock Text="API Key"/>
                        <PasswordBox x:Name="ApiKeyPasswordBox"/>

                        <TextBlock Text="模型"/>
                        <TextBox x:Name="ModelTextBox"/>

                        <CheckBox x:Name="UseDefaultCheckBox"
                                  Content="留空时使用默认限免"
                                  Margin="0,0,0,12"/>
                    </StackPanel>
                </Border>

                <TextBlock x:Name="StatusTextBlock"
                           TextWrapping="Wrap"
                           Foreground="#475467"
                           Margin="2,14,2,0"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="测试 API" Click="TestApiButton_Click"/>
            <Button Content="开始截图" Click="StartCaptureButton_Click"/>
            <Button Content="保存" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
